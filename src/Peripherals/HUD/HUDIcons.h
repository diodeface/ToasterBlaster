#pragma once

#include "Utilities/Typedefs.h"
#include <Arduino.h>

/**
 * Bitmaps used in heads up display.
 */
namespace HUDIcons {
    extern Bitmap 
        microphone[8], 
        noBoop[8], 
        boop[8], 
        led[8], 
        bluetooth[8], 
        tick[8], 
        cross[8], 
        load[8];
}